{
  "name": "login-service",
  "version": "1.0.0",
  "private": true,
  "description": "Microsservico intermediario de login que chama um Auth Service externo",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "rabbit:email-validate": "node scripts/send-email-validate.js",
    "rabbit:test": "node scripts/send-rabbit-message.js",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage"
  },
  "keywords": [
    "login",
    "express",
    "microservice",
    "oauth2"
  ],
  "license": "ISC",
  "dependencies": {
    "amqplib": "^2.0.1",
    "axios": "^1.7.9",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
